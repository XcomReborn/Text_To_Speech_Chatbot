<Page x:Class="WpfApp1.Pages.FrontPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FrontPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="77*"/>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <Button Grid.Column="0" Grid.Row="1" x:Name="connectButton" MaxHeight="50" MaxWidth="200" Click="OnClickConnectButton" Margin="0,0,0,10"/>

        <!-- Information Pannel For Debugging only-->
        <TextBlock  x:Name="consoleDebugText" Grid.Column="0" Grid.Row="2" TextWrapping="Wrap" Text="" Foreground="Red"/>




    </Grid>
</Page>
